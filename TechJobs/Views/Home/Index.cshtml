@{ 
    ViewBag.Title = "Home";
}
<p>View jobs by:</p>
<ul>
    @foreach (KeyValuePair<string, string> action in ViewBag.actions)
    {
        <li><a class="text-info" href="/@action.Key">@action.Value</a></li>
    }
</ul>